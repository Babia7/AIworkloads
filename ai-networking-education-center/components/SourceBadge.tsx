import React from 'react';
import type { SourceLinkedValue } from '../types';

interface SourceBadgeProps {
  claim: SourceLinkedValue;
  className?: string;
}

const SourceBadge: React.FC<SourceBadgeProps> = () => null;

export default SourceBadge;
